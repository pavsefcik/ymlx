{
  "name": "ymlx",
  "version": "0.2.0",
  "description": "Local MLX LLM manager for Apple Silicon, plus the ymlx-sync pi extension (model sync + headless `ymlx run` on :11500)",
  "license": "MIT",
  "keywords": ["pi-package", "mlx", "llm", "local-ai"],
  "type": "module",
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": ["./extensions"]
  }
}